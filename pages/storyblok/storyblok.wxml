<template name="grid">
  <grid columns="{{columns}}"></grid>
</template>

<template name="product">
  <product title="{{title}}" url="{{cover.filename}}"></product>
</template>

<block wx:for="{{season.bloks}}" wx:key="_uid">
  <template is="{{item.component}}" data="{{...item}}" />
</block>